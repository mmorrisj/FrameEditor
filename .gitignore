# Virtualenv (recreate with: python -m venv venv && venv/bin/pip install -r requirements.txt)
venv/

# Python bytecode
__pycache__/
*.py[cod]

# Uploaded videos, extracted frames, outputs — user data, potentially huge
projects/
